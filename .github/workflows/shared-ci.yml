name: Shared CI

on:
  pull_request:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gitleaks:
    uses: sigiuscom/workflows/.github/workflows/gitleaks.yml@main
    secrets: inherit

  opengrep:
    uses: sigiuscom/workflows/.github/workflows/opengrep.yml@main
    with:
      config: |
        p/typescript
        p/javascript
        p/react
        p/owasp-top-ten
        p/secrets

  node:
    uses: sigiuscom/workflows/.github/workflows/node-app.yml@main
    with:
      package-manager: npm
      typecheck-cmd: 'npx tsc --noEmit'