name: Shared CI

on:
  pull_request:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  actionlint:
    uses: sigiuscom/workflows/.github/workflows/actionlint.yml@96c8930ba5571658047132774b0d32ceee319a00

  gitleaks:
    uses: sigiuscom/workflows/.github/workflows/gitleaks.yml@13300b068bd61965e241b6d7ae4ba767fcc481d3 # main

  opengrep:
    uses: sigiuscom/workflows/.github/workflows/opengrep.yml@13300b068bd61965e241b6d7ae4ba767fcc481d3 # main
    with:
      config: |
        p/typescript
        p/javascript
        p/react
        p/owasp-top-ten
        p/secrets

  node:
    uses: sigiuscom/workflows/.github/workflows/node-app.yml@13300b068bd61965e241b6d7ae4ba767fcc481d3 # main
    with:
      package-manager: npm
      typecheck-cmd: 'npx tsc --noEmit'